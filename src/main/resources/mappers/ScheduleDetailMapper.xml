<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.www.triptrav.repository.ScheduleDetailMapper">
    <insert id="insertDetailPlan">
        insert into schedule_detail (sco, sche_content_id, sche_index, sche_date)
        values (#{sco}, #{contentId},#{i}, #{j})
    </insert>

    <insert id="updatePlan">
        insert into schedule_detail (sche_content_id, sche_index, sche_date, sco)
        VALUES (#{scheContentId}, #{scheIndex}, #{scheDate}, #{sco})
        ON DUPLICATE KEY UPDATE
            sche_content_id = VALUES(sche_content_id),
            sche_index = VALUES(sche_index),
            sche_date = VALUES(sche_date);
    </insert>
</mapper>