<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.www.triptrav.repository.PlanMapper">
    <insert id="insertPlan">
        insert into schedule (sche_name, uno, sche_start, sche_end, sche_count)
        values (#{sche_name}, #{uno}, #{sche_start}, #{sche_end}, #{sche_count})
    </insert>
</mapper>